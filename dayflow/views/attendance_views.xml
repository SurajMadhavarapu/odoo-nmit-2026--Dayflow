<?xml version="1.0" encoding="utf-8"?>
<!--
    Dayflow Attendance Views
    Owner: KUNAM (Attendance & Leave workflow/UI)
    Stub author: Mani — handed off. Extend or replace in feature/kunam-*.

    Field reference (dayflow.attendance):
        employee_id, date, check_in, check_out, worked_hours, status, notes
    Status values: present | absent | half_day | leave
    Actions: action_check_in(), action_check_out()
-->
<odoo>
    <data>

        <!-- ============================================================
             ATTENDANCE — List View
        ============================================================ -->
        <record id="view_dayflow_attendance_tree" model="ir.ui.view">
            <field name="name">dayflow.attendance.tree</field>
            <field name="model">dayflow.attendance</field>
            <field name="arch" type="xml">
                <tree string="Attendance Records"
                      decoration-success="status == 'present'"
                      decoration-warning="status == 'half_day'"
                      decoration-danger="status == 'absent'"
                      decoration-info="status == 'leave'">
                    <field name="employee_id"/>
                    <field name="date"/>
                    <field name="check_in"/>
                    <field name="check_out"/>
                    <field name="worked_hours" widget="float_time"/>
                    <field name="status"/>
                </tree>
            </field>
        </record>

        <!-- ============================================================
             ATTENDANCE — Form View
        ============================================================ -->
        <record id="view_dayflow_attendance_form" model="ir.ui.view">
            <field name="name">dayflow.attendance.form</field>
            <field name="model">dayflow.attendance</field>
            <field name="arch" type="xml">
                <form string="Attendance Record">
                    <header>
                        <!-- Check In/Out buttons — employee uses these -->
                        <button name="action_check_in"
                                string="Check In"
                                type="object"
                                class="btn-primary"
                                attrs="{'invisible': [('check_in', '!=', False)]}"/>
                        <button name="action_check_out"
                                string="Check Out"
                                type="object"
                                class="btn-success"
                                attrs="{'invisible': ['|', ('check_in', '=', False), ('check_out', '!=', False)]}"/>
                        <field name="status" widget="statusbar"
                               statusbar_visible="present,half_day,absent,leave"/>
                    </header>
                    <sheet>
                        <group>
                            <group string="Employee">
                                <field name="employee_id"/>
                                <field name="date"/>
                            </group>
                            <group string="Times">
                                <field name="check_in"/>
                                <field name="check_out"/>
                                <field name="worked_hours" widget="float_time"/>
                            </group>
                        </group>
                        <group string="Notes">
                            <field name="notes" nolabel="1" placeholder="Optional notes..."/>
                        </group>
                    </sheet>
                </form>
            </field>
        </record>

        <!-- ============================================================
             ATTENDANCE — Search View
        ============================================================ -->
        <record id="view_dayflow_attendance_search" model="ir.ui.view">
            <field name="name">dayflow.attendance.search</field>
            <field name="model">dayflow.attendance</field>
            <field name="arch" type="xml">
                <search string="Search Attendance">
                    <field name="employee_id"/>
                    <field name="date"/>
                    <field name="status"/>
                    <filter string="Today" name="filter_today"
                            domain="[('date', '=', context_today().strftime('%Y-%m-%d'))]"/>
                    <filter string="Present"  name="filter_present"
                            domain="[('status', '=', 'present')]"/>
                    <filter string="Absent"   name="filter_absent"
                            domain="[('status', '=', 'absent')]"/>
                    <filter string="Half Day" name="filter_halfday"
                            domain="[('status', '=', 'half_day')]"/>
                    <filter string="On Leave" name="filter_leave"
                            domain="[('status', '=', 'leave')]"/>
                    <separator/>
                    <filter string="My Attendance" name="filter_mine"
                            domain="[('employee_id.user_id', '=', uid)]"/>
                    <group expand="0" string="Group By">
                        <filter string="Employee" name="group_employee"
                                context="{'group_by': 'employee_id'}"/>
                        <filter string="Date"     name="group_date"
                                context="{'group_by': 'date'}"/>
                        <filter string="Status"   name="group_status"
                                context="{'group_by': 'status'}"/>
                    </group>
                </search>
            </field>
        </record>

        <!-- ============================================================
             ATTENDANCE — Window Action
        ============================================================ -->
        <record id="action_dayflow_attendance" model="ir.actions.act_window">
            <field name="name">Attendance</field>
            <field name="res_model">dayflow.attendance</field>
            <field name="view_mode">tree,form</field>
            <field name="search_view_id" ref="view_dayflow_attendance_search"/>
            <field name="context">{'search_default_filter_today': 1}</field>
            <field name="help" type="html">
                <p class="o_view_nocontent_smiling_face">
                    No attendance records found.
                </p>
            </field>
        </record>

        <!-- My Attendance — pre-filtered to logged-in employee -->
        <record id="action_dayflow_my_attendance" model="ir.actions.act_window">
            <field name="name">My Attendance</field>
            <field name="res_model">dayflow.attendance</field>
            <field name="view_mode">tree,form</field>
            <field name="search_view_id" ref="view_dayflow_attendance_search"/>
            <field name="context">{'search_default_filter_mine': 1}</field>
        </record>

    </data>
</odoo>
